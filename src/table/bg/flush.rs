pub struct Flush {}

impl Flush {
    pub async fn run(mut self) {
        'OUTER: loop {
            // do cleanup.

            // if shutdown {
            //    break 'OUTER;
            //          }
        }
    }
}
