pub struct Flush {}

impl Flush {
    pub(crate) async fn run(mut self) {
        'OUTER: loop {
            // do cleanup.

            // if shutdown {
            //    break 'OUTER;
            //          }
        }
    }
}
